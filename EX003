(define str "helloworld")
(define i 5)

(string-append ( substring str 0 i) "_" ( substring str i 10))

;or 


(define str "helloworld")
(define i 5)
(define strlen (string-length "helloworld"))

(string-append ( substring str 0 i) "_" ( substring str i strlen))
